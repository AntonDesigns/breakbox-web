# ---------- .NET / Visual Studio ----------
bin/
obj/
*.user
*.suo
*.userosscache
.vs/
[Dd]ebug/
[Rr]elease/
x64/
x86/
publish/
*.publishsettings

# ---------- Node / frontend ----------
node_modules/
dist/
dist-ssr/
*.local
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# ---------- Env / secrets (never commit these) ----------
.env
.env.*
!.env.example

# ---------- Local tooling (not part of the project) ----------
.claude/
.vscode/

# ---------- OS / editor ----------
.DS_Store
Thumbs.db
*.swp
.idea/

# ---------- Generated Word docs (kept local, not committed) ----------
*.docx
*.docx

# ---------- Local-only notes (kept on disk, not committed) ----------
docs/
